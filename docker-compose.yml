networks:
  saas_network:
    external: true
    name: saas_network

services:
  service-bus:
    build:
      context: ..
      dockerfile: service_bus/Dockerfile
    container_name: service_bus
    ports:
      - "8081:8081"
    environment:
      AUTH_SERVICE_URL: http://auth-service:8080
      JWT_SECRET: local-dev-secret-key-change-in-production-32ch
    networks:
      - saas_network
